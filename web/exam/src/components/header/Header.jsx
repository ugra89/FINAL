import { Typography } from '@mui/material';

const Header = () => {
  return (
    <div>
      <Typography variant='h2' align='center'>
        header
      </Typography>
    </div>
  );
};

export default Header;
